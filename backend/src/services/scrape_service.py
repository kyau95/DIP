from src.scrapers.adapter import BaseAdapater, PlaywrightAdapter


# Scrape the price, the POST from api/products will call this function
# Need to think about if the user has added the same item to the DB, we should update the item in the DB

